# GCP
GCP_PROJECT_ID=your-project-id
GCS_BUCKET=your-project-id-taxi-lake
BQ_DATASET_RAW=raw
BQ_LOCATION=US

# Data
TAXI_DATA_DIR=./data
# Month to ingest, format YYYY-MM (drives the yellow_tripdata_YYYY-MM.parquet
# filename). One month is a deliberately small slice to stay in the free tier.
TAXI_MONTH=2023-01
